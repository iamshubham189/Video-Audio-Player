# Audio/Video Player using (React + Vite)

## Show to start the project

```
Install the dependencies --> npm install
Start the project --> npm run dev
Build the project --> npm run build
Preview the build --> npm run preview

```
